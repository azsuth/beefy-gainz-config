spring:
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: jdbc:postgresql://exercise-db:5432/exercise
    username: exercise_user
    password: exercise_temp_password
  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
    # properties:
    #   javax:
    #     persistence:
    #       schema-generation:
    #         create-source: metadata
    #         scripts:
    #           action: create
    #           create-target: create.sql
